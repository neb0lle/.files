# For the benefit of emacs users: -*- shell-script -*-
###########################
# xbindkeys configuration #
###########################
#
# Version: 1.8.6
#
# If you edit this file, do not forget to uncomment any lines
# that you change.
# The pound(#) symbol may be used anywhere for comments.
#
# To specify a key, you can use 'xbindkeys --key' or
# 'xbindkeys --multikey' and put one of the two lines in this file.
#
# The format of a command line is:
#    "command to start"
#       associated key
#
# A list of keys is in /usr/include/X11/keysym.h and in
# /usr/include/X11/keysymdef.h
# The XK_ is not needed.
# List of modifier: Release, Control, Shift, Mod1 (Alt), Mod2 (NumLock),
#   Mod3 (CapsLock), Mod4, Mod5 (Scroll).
#

# The release modifier is not a standard X modifier, but you can
# use it if you want to catch release events instead of press events

# By defaults, xbindkeys does not pay attention with the modifiers
# NumLock, CapsLock and ScrollLock.
# Uncomment the lines above if you want to pay attention to them.

#keystate_numlock = enable
#keystate_capslock = enable
#keystate_scrolllock= enable

# Audio
"pactl -- set-sink-volume @DEFAULT_SINK@ +5%"
   XF86AudioRaiseVolume
"pactl -- set-sink-volume @DEFAULT_SINK@ -5%"
   XF86AudioLowerVolume
"pactl -- set-sink-mute @DEFAULT_SINK@ toggle"
   XF86AudioMute

# PlayerCTL
"playerctl --all-players next"
    XF86AudioNext
"playerctl --all-players previous"
    XF86AudioPrev
"playerctl --all-players play-pause"
    XF86AudioPlay
"playerctl --all-players stop"
    XF86AudioStop

# ROFI
"/home/neville/.config/rofi/scripts/powermenu.sh"
    Mod4+Escape
"rofi -theme '~/.config/rofi/themes/sed.rasi' -combi-modi window,drun -show combi -modi combi -window-format {t} -show-icons -display-combi 'run:'"
    Mod4+space
"""rofi -theme '~/.config/rofi/themes/sed.rasi' -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'"""
    Mod4+c
"""rofi -show calc -modi calc -no-show-match -no-sort -theme ~/.config/rofi/themes/sed.rasi -calc-command 'echo -n "{result}" | xclip -selection clipboard' -display-calc 'calculator:'"""
    Mod4+equal
"rofi-pass"
    Mod4+p
# """rofi -show window -sort -theme "~/.config/rofi/themes/sed.rasi" -window-format "{t:10}" -kb-accept-entry "!Alt-Tab" -kb-row-down Alt-Tab -display-window "cycle:""""
#     Alt+Tab

# Application Bindings
"kitty"
    Mod4+Return
"flameshot gui"
    control+Print
"flameshot full -c"
    Print
"brave"
    Mod4+b
"nemo"
    Mod4+f

##################################
# End of xbindkeys configuration #
##################################
